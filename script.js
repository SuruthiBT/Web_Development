
   console.log("Welcome to my journey!"); 
/*
 <script>
  function navigate(event, link) {
    event.preventDefault(); // stop instant jump

    const overlay = document.querySelector(".transition");
    overlay.classList.add("active");

    setTimeout(() => {
      window.location.href = link.href;
    }, 600); // match CSS timing
  }

  // Page load animation (reverse effect)
  window.onload = () => {
    const overlay = document.querySelector(".transition");
    overlay.style.transform = "translateY(0)";
    
    setTimeout(() => {
      overlay.style.transform = "translateY(-100%)";
    }, 100);
  };
</script> */